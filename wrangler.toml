name = "d2theorycraft"
compatibility_date = "2024-09-23"

# Serve the fully static Next.js export from the out/ directory.
# No Worker code — pure static asset hosting via Cloudflare's asset pipeline.
[assets]
directory = "./out"
